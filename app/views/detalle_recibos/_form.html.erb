<%= form_for(@detalle_recibo) do |f| %>
  <% if @detalle_recibo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detalle_recibo.errors.count, "error") %> prohibited this detalle_recibo from being saved:</h2>

      <ul>
      <% @detalle_recibo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recibo_id %><br>
    <%= f.number_field :recibo_id %>
  </div>
  <div class="field">
    <%= f.label :cuota %><br>
    <%= f.number_field :cuota %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
